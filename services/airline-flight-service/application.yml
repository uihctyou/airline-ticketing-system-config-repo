# Global
spring:
  cloud:
    config:
      uri: http://config-server:8889  # Config Server address
    bus:
      enabled: true                   

logging:
  level:
    root: INFO

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/